# ======================
# IMAP Connection Settings
# ======================
# IMAP server to connect to
# IMAP settings
IMAP_SERVER=mail.1click.software
IMAP_PORT=143
IMAP_SECURE=starttls   # options: ssl, starttls, none
IMAP_USER=youruser
IMAP_PASS=yourpass

# IMAP username and password for authentication
IMAP_USER=bounce@yourdomain.com
IMAP_PASS="replace_with_secure_password"

# ======================
# SMTP Settings (used for notifications + summary emails)
# ======================
SMTP_SERVER=smtp.yourmail.com
SMTP_PORT=587
SMTP_USER=notifications@yourdomain.com
SMTP_PASS="replace_with_secure_password"

# ======================
# Notification Recipients
# ======================
# All bounce notifications + daily summary will be CC'd here
NOTIFY_CC=support@yourdomain.com
NOTIFY_CC_TEST=support@yourdomain.com
# ======================
# IMAP Folders (Normal Mode)
# ======================
# Incoming new messages arrive here
IMAP_FOLDER_INBOX=INBOX

# Processed bounce messages go here after handling
IMAP_FOLDER_PROCESSED=PROCESSED

# Messages that could not be processed (even after retries) go here
IMAP_FOLDER_PROBLEM=PROBLEM

# Messages that are not bounces (e.g. auto-replies) are moved here
IMAP_FOLDER_SKIPPED=SKIPPED

# ======================
# IMAP Folders (Test Mode)
# ======================
# In test mode, the processor will use these folders instead
IMAP_FOLDER_TEST=TEST
IMAP_FOLDER_TESTPROCESSED=TESTPROCESSED
IMAP_FOLDER_TESTPROBLEM=TESTPROBLEM
IMAP_FOLDER_TESTSKIPPED=TESTSKIPPED

# Toggle test mode (true/false)
IMAP_TEST_MODE=true

# ======================
# Web Dashboard Settings
# ======================
# Port to expose the HTTPS dashboard
WEBUI_HTTPS_PORT=8888

# Password for dashboard login (any username works, only password checked)
ADMIN_PASS="changeme"

# ======================
# Optional: Use real certificates instead of the built-in self-signed cert
# SSL_KEY=/data/certs/server.key
# SSL_CRT=/data/certs/server.crt
#
# If unset, a self-signed certificate is generated at container build time.

# ======================
# HTTP Security
# ======================
# Strict-Transport-Security (HSTS) settings
HSTS_MAX_AGE=15552000        # ~180 days
HSTS_INCLUDE_SUBDOMAINS=true # apply to all subdomains